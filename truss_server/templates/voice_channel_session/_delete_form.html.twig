<form method="post" action="{{ path('app_voice_channel_session_delete', {'id': voice_channel_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voice_channel_session.id) }}">
    <button class="btn">Delete</button>
</form>
