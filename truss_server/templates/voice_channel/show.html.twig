{% extends 'base.html.twig' %}

{% block title %}VoiceChannel{% endblock %}

{% block body %}
    <h1>VoiceChannel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voice_channel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ voice_channel.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ voice_channel.createdAt ? voice_channel.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ voice_channel.updatedAt ? voice_channel.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voice_channel_index') }}">back to list</a>

    <a href="{{ path('app_voice_channel_edit', {'id': voice_channel.id}) }}">edit</a>

    {{ include('voice_channel/_delete_form.html.twig') }}
{% endblock %}
